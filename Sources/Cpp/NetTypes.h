
enum class NetworkProtocol
{
    ICMP,
    ICMPv6,
    IPv4,
    IPv6,
};

enum class TransportProtocol
{
    TCP,
    UDP,
    DCCP,
    SCTP
};

enum class ApplicationProtocol
{
    BGP,
    DNS,
    FTP,
    HTTP,
    HTTPS,
    SSH,
    SMTP,
    Telnet,
    TLS_SSL,
};